1.文件的基本操作
    read
    write
    tell 取出光标位置方法
    seek 断点续传 查找光标所在的位置
    flush
    eval()转回原有的数据类型
2.三级菜单(数据存在文件中)
    对菜单实现增删改

3.虚拟机的按照Ubuntu(未学习)
    vmware 软件安装

4.复习之前的内容
    1.第一个程序  文件以.py结尾
        print('hello world')
    2.变量

   ........
5.深浅拷贝
    浅拷贝只copy 第一层
    深copy 完全copy复制一份
6.set集合(重点)
    特点:
        去重
        可哈希的(不可变的)
        s.add()只能增加一个元素
        .update 将所有字符当成多个元素加入到集合中
        .pop()随机删除
        .clear 清空
        del.s 完全删除
    集合类型操作符
        in  not in
        等价 完全一样
        .issuperset()  父集
        .issubset()   子集
        丨 并集
        ^ 差集
7.Frozenset 不可变集合